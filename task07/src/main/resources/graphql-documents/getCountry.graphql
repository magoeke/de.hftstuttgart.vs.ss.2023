query getCountry($countryCode: ID!, $lang: String) {
    country(code: $countryCode) {
        name(lang: $lang)
        capital
        emoji
        languages {
            name
            code
        }
        currencies
        phones
    }
}
